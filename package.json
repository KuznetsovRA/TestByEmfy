{
  "name": "Proxy-server",
  "version": "1.0.0",
  "main": "api/deals.js",
  "scripts": {
    "start": "node api/deals.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
